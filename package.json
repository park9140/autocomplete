{
  "name": "autocomplete",
  "version": "0.28.0",
  "main": "./lib/autocomplete",
  "suggestion-providers": [
    "lib/suggestion-providers/buffer",
    "lib/suggestion-providers/scope"
  ],
  "description": "Display possible completions in the editor with `ctrl-space`.",
  "activationEvents": {
    "autocomplete:toggle": ".editor"
  },
  "repository": "https://github.com/atom/autocomplete",
  "engines": {
    "atom": "*"
  },
  "dependencies": {
    "async": "^0.9.0",
    "emissary": "^1.2.1",
    "underscore-plus": "1.x"
  }
}
